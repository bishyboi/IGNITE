python -c "import cv2; cap = cv2.VideoCapture(1, cv2.CAP_DSHOW); cap.set(cv2.CAP_PROP_AUTO_EXPOSURE, 0.25); cap.set(cv2.CAP_PROP_EXPOSURE, -10); print('exposure:', cap.get(cv2.CAP_PROP_EXPOSURE)); input('Press Enter to release cam...')"
